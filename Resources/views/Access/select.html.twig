
<form action="" method="post">

    <div class="gitlab_select_div">
        <label>Gitlab account</label>
        <select id="gitlab_access_selector" name="access_id">
            {% for data in accessData %}
                <option value="{{ data.id }}"{% if data.id == access.id %} selected="selected"{% endif %}>
                    {{ data }}
                </option>
            {% endfor %}
        </select>
        <input type="submit" value="select" />
    </div>

</form>

<div class="gitlab_select_div">
    <label>Gitlab project</label>
    <select id="gitlab_project_selector">
        {% for data in projects %}
            <option>{{ data.name }}</option>
        {% endfor %}
    </select>
</div>
